import { DashboardClient } from "@/components/dashboard/DashboardClient";

export const metadata = { title: "Monitoring dashboard — UzLogistics" };

export default function DashboardPage() {
  return <DashboardClient />;
}
