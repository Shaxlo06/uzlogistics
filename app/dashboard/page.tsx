import { DashboardClient } from "@/components/dashboard/DashboardClient";

export const metadata = { title: "Monitoring dashboard — uzlogisticsnet" };

export default function DashboardPage() {
  return <DashboardClient />;
}
