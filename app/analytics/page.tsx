import { AnalyticsClient } from "@/components/analytics/AnalyticsClient";

export const metadata = { title: "KPI tahlili — UzLogistics" };

export default function AnalyticsPage() {
  return <AnalyticsClient />;
}
